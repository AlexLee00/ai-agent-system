// @ts-nocheck
'use strict';

// Canonical current module name. The shared legacy rail still lives in
// mainbot-client.legacy.js until the Claude compatibility layer is retired.
module.exports = require('./mainbot-client.legacy.js');
