# Gotchas

- `llm_selector_chain_required` appeared 47 times in 7 days before the default guarantee.
- Public and investment routing logs have diverged column sets.
- Four `claude alarm` mirror rows showed over-specific fingerprinting.
- Darwin produced a large signal volume without enough Hub-level detection.
- p95 around 65s can hide timeout/profile mistakes.
- `hub._default` is not a bypass for non-LLM or target-policy blocks.
- Shadow auto-routing is observation only.
