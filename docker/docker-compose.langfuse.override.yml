# LANGFUSE_INIT_* 자동 초기화 + ClickHouse 단일 인스턴스 모드 override
# 메티 (2026-05-14): ORG_ID + PROJECT_ID 추가 (자동 초기화 활성화)

services:
  langfuse-web:
    environment:
      CLICKHOUSE_CLUSTER_ENABLED: "false"
      # 자동 초기화 (ORG_ID + PROJECT_ID 필수)
      LANGFUSE_INIT_ORG_ID: ${LANGFUSE_INIT_ORG_ID:-team-jay-org-id}
      LANGFUSE_INIT_PROJECT_ID: ${LANGFUSE_INIT_PROJECT_ID:-team-jay-prod-id}
      LANGFUSE_INIT_USER_EMAIL: ${LANGFUSE_INIT_USER_EMAIL:-}
      LANGFUSE_INIT_USER_PASSWORD: ${LANGFUSE_INIT_USER_PASSWORD:-}
      LANGFUSE_INIT_USER_NAME: ${LANGFUSE_INIT_USER_NAME:-}
      LANGFUSE_INIT_ORG_NAME: ${LANGFUSE_INIT_ORG_NAME:-}
      LANGFUSE_INIT_ORG_CLOUD_PLAN: ${LANGFUSE_INIT_ORG_CLOUD_PLAN:-}
      LANGFUSE_INIT_PROJECT_NAME: ${LANGFUSE_INIT_PROJECT_NAME:-}
      LANGFUSE_INIT_PROJECT_PUBLIC_KEY: ${LANGFUSE_INIT_PROJECT_PUBLIC_KEY:-}
      LANGFUSE_INIT_PROJECT_SECRET_KEY: ${LANGFUSE_INIT_PROJECT_SECRET_KEY:-}

  langfuse-worker:
    environment:
      CLICKHOUSE_CLUSTER_ENABLED: "false"
