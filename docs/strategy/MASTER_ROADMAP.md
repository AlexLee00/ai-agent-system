# 팀 제이 통합 실행 계획 — 기존 PENDING + CC 개선안 종합

> 작성: 메티 (Claude Opus 4.6)
> 작성일: 2026-04-04
> 목적: 모든 미완료 작업 + CC에서 도출된 개선안을 하나로 통합
> 원칙: 함께 진행할 수 있는 것은 묶고, 분리할 것은 분리

---

## 1. 완료 현황 요약

```
✅ Phase A 블로팀 기반안정화 (A-1 ISBN, A-2 이미지, A-3 공용화)
✅ 90에이전트 + 9팀 구조 (Phase 0~3)
✅ 경쟁 시스템 활성화 (월수금)
✅ JSONB 전환 (PhaseB-1)
✅ 스킬/MCP/도구 시스템 (Phase 6)
✅ LLM 모델 재편성 프롬프트 완료
✅ Hub Secrets Store 아키텍처 완료
✅ DEV↔OPS 환경 분리 완성
✅ OpenClaw Phase 1~3 완료
✅ 로컬 LLM + 임베딩 전환 ($0)
✅ CC 유출 종합 연구 + 개선안 14건 도출
```

---

## 2. 통합 실행 계획

### ━━━ W1: 이번 주 (04-04~06) — 즉시 실행 ━━━

```
묶음 A: 코어 안정성 강화 [CC P0 + 기존]
  ┌─────────────────────────────────────────────────────┐
  │ A-1. 연속 실패 제한 (CC에서 도출)                    │ NEW
  │   파일: llm-fallback.js                              │
  │   내용: MAX_CONSECUTIVE_FAILURES=5, 3줄 추가         │
  │   효과: 무한 재시도 방지 (CC에서 25만 호출/일 절약)   │
  │                                                      │
  │ A-2. Strict Write Discipline (CC에서 도출)            │ NEW
  │   파일: rag.js, self-improving 관련                   │
  │   내용: 성공 확인 후에만 메모리 저장                   │
  │   효과: 잘못된 학습 방지                              │
  │                                                      │
  │ A-3. LLM 모델 재편성 수정 2건 (기존 PENDING)          │ EXISTING
  │   oracle F1중복 + worker local 미삽입 검증             │
  │                                                      │
  │ → 코덱스 프롬프트 1개로 묶어서 진행!                  │
  └─────────────────────────────────────────────────────┘
```

### ━━━ W2: 다음 주 (04-07~11) — 블로팀 Phase B + 코어 ━━━

```
묶음 B: 블로팀 Phase B 피드백 루프 [기존 계획]
  ┌─────────────────────────────────────────────────────┐
  │ B-1. 성과 수집 → RAG 저장                            │ EXISTING
  │ B-2. 생성 시 과거 성과 반영                           │ EXISTING
  │ B-3. Standing Orders 승격                            │ EXISTING
  │ B-4. 성과 대시보드                                   │ EXISTING
  │                                                      │
  │ + CC 패턴 추가: 경쟁 결과→RAG 피드백 루프             │ NEW
  │   (경쟁 승패 이유를 RAG에 저장→다음 경쟁에 참조)      │
  │                                                      │
  │ → 블로팀 피드백 + 경쟁 피드백을 함께 구현!            │
  └─────────────────────────────────────────────────────┘

묶음 C: 야간 자율 시스템 [CC + 기존 합체!]
  ┌─────────────────────────────────────────────────────┐
  │ C-1. 야간 메모리 증류 (CC autoDream 패턴)            │ NEW
  │   파일: scripts/nightly-distill.js (신규)             │
  │   내용: 하루 로그→패턴 추출→모순 해소→memory.md 갱신  │
  │                                                      │
  │ C-2. Doctor 예방적 스캔 (CC KAIROS 패턴)             │ NEW
  │   파일: bots/claude/lib/doctor.js                    │
  │   내용: 에러 발생 전 경고 징후 탐지                   │
  │                                                      │
  │ → night-handler + Doctor + 야간증류를 하나로 묶음!    │
  │ → 이미 night-handler가 KAIROS와 유사 → 여기에 추가   │
  └─────────────────────────────────────────────────────┘

별도 D: 첫 경쟁 결과 확인 + Gemma 4 Ollama 테스트 [기존]
  ┌─────────────────────────────────────────────────────┐
  │ D-1. 월요일 첫 경쟁 결과 확인                        │ EXISTING
  │ D-2. Gemma 4 Ollama 테스트 (e4b + 26b MoE)          │ EXISTING
  │ D-3. Phase 4 검증 (alert resolve OpenClaw 통합)      │ EXISTING
  │                                                      │
  │ → 각각 별도 진행 (의존성 없음)                        │
  └─────────────────────────────────────────────────────┘
```

### ━━━ W3~4: (04-14~25) — 품질 + 보안 강화 ━━━

```
묶음 E: 도구/권한 체계화 [CC + 기존 합체!]
  ┌─────────────────────────────────────────────────────┐
  │ E-1. 도구별 권한 레이어 (CC 패턴)                    │ NEW
  │   파일: skill-selector.js, tool-selector.js          │
  │   내용: permission 필드 (auto/approve/block)          │
  │                                                      │
  │ E-2. 팀별 도구 서브셋 (Vercel 80% 제거 패턴)         │ NEW
  │   파일: runtime-profiles.js                          │
  │   내용: 팀별 활성 도구 목록 제한                      │
  │                                                      │
  │ → 이미 있는 skill-selector + runtime-profiles에 추가! │
  └─────────────────────────────────────────────────────┘

묶음 F: 블로팀 Phase C — SEO+GEO [기존 계획]
  ┌─────────────────────────────────────────────────────┐
  │ F-1. 네이버 키워드 API 연동                          │ EXISTING
  │ F-2. SEO 스킬                                       │ EXISTING
  │ F-3. GEO 스킬                                       │ EXISTING
  │ F-4. 이중 채점                                      │ EXISTING
  └─────────────────────────────────────────────────────┘

묶음 G: 대규모 파일 리팩토링 [CC 교훈 + 기존]
  ┌─────────────────────────────────────────────────────┐
  │ G-1. forecast.py 2,047줄 → 3파일 분리                │ NEW
  │ G-2. chat-agent.js 876줄 → 3파일 분리                │ NEW
  │ G-3. edl-builder.js 971줄 → 분리                     │ NEW
  │ G-4. blo.js 991줄 → 분리 (A-3에서 시작, 계속)        │ STARTED
  │                                                      │
  │ → 각 팀별 별도 프롬프트로 분리 진행                   │
  └─────────────────────────────────────────────────────┘
```

### ━━━ W5~8: (04-28~05-23) — 아키텍처 진화 ━━━

```
묶음 H: 컨텍스트 관리 [CC 최대 Gap]
  ┌─────────────────────────────────────────────────────┐
  │ H-1. MicroCompact — 도구 결과 로컬 트리밍            │ NEW
  │   파일: packages/core/lib/context-compactor.js (신규) │
  │                                                      │
  │ H-2. AutoCompact — 컨텍스트 상한 시 LLM 요약 생성    │ NEW
  │                                                      │
  │ → 루나(실시간)+블로(일일)+클로드(모니터링) 전팀 영향  │
  └─────────────────────────────────────────────────────┘

묶음 I: 에이전트 간 협업 강화 [CC + 기존]
  ┌─────────────────────────────────────────────────────┐
  │ I-1. 루나 독립 노드 병렬화 (l03+l04+l05)             │ NEW
  │ I-2. Mailbox 패턴 — approval-queue.js                │ NEW
  │ I-3. AgentTool — agent-tool.js (에이전트 간 위임)     │ NEW
  │                                                      │
  │ → I-1은 루나팀 단독, I-2/I-3은 코어 모듈             │
  └─────────────────────────────────────────────────────┘

묶음 J: 비디오팀 Phase 3 [기존 계획]
  ┌─────────────────────────────────────────────────────┐
  │ J-1. CapCut급 타임라인 UI (Twick)                    │ EXISTING
  │ J-2. AI 스텝바이스텝 편집                            │ EXISTING
  │ J-3. RED/BLUE 품질검증 + 피드백 RAG                  │ EXISTING
  │                                                      │
  │ + CC 패턴: critic-agent 프롬프트 기반 전환            │ NEW
  │ + CC 패턴: 편집 패턴 RAG 학습                        │ NEW
  └─────────────────────────────────────────────────────┘

별도 K: 에이전트 오피스 CC 메트릭 대시보드 [CC]
  ┌─────────────────────────────────────────────────────┐
  │ K-1. 에이전트별 토큰 사용량/비용 그래프               │ NEW
  │ K-2. 실패율 + 평균 응답시간 차트                      │ NEW
  │ K-3. 경쟁 승률 + 감독 레벨 시각화                    │ NEW
  └─────────────────────────────────────────────────────┘
```

### ━━━ M3+: (05-26~) — 자율 진화 ━━━

```
묶음 L: 자율 운영 [CC 장기 + 기존]
  ┌─────────────────────────────────────────────────────┐
  │ L-1. KAIROS 유사 자율 데몬 — 5분 주기 모니터링       │ NEW
  │ L-2. 프롬프트 기반 오케스트레이션 — 코드→프롬프트     │ NEW
  │ L-3. Build to Delete 아키텍처 — 모듈화 원칙          │ NEW
  │ L-4. 블로팀 Phase E 자율 진화                        │ EXISTING
  │ L-5. Chronos Layer 2~3 본격 운영                     │ EXISTING
  │ L-6. 일일성장 환류 사이클 9팀 적용                    │ EXISTING
  └─────────────────────────────────────────────────────┘
```

---

## 3. 우선순위 요약 — 한눈에 보기

```
┌────┬──────────────────────────────┬────────┬──────────┬────────┐
│순서│ 작업                         │ 출처   │ 일정     │ 난이도 │
├────┼──────────────────────────────┼────────┼──────────┼────────┤
│ 1  │ 연속실패제한+StrictWrite      │ CC P0  │ W1 즉시  │ ★☆☆  │
│ 2  │ LLM모델재편성 수정2건         │ 기존   │ W1 즉시  │ ★☆☆  │
│ 3  │ 블로팀 Phase B 피드백루프     │ 기존   │ W2       │ ★★☆  │
│ 4  │ 경쟁결과→RAG 피드백           │ CC+기존│ W2 합체  │ ★★☆  │
│ 5  │ 야간메모리증류+Doctor예방     │ CC P1  │ W2       │ ★★☆  │
│ 6  │ 첫경쟁확인+Gemma4+Phase4     │ 기존   │ W2 별도  │ ★☆☆  │
│ 7  │ 도구별 권한+팀별 서브셋       │ CC P1  │ W3       │ ★★☆  │
│ 8  │ 블로팀 Phase C SEO+GEO       │ 기존   │ W3~4     │ ★★☆  │
│ 9  │ 대규모파일 리팩토링 4건       │ CC교훈 │ W3~4     │ ★★☆  │
│10  │ 컨텍스트 압축 Micro+Auto      │ CC P2  │ W5~6     │ ★★★  │
│11  │ 루나 노드 병렬화              │ CC P1  │ W5       │ ★★☆  │
│12  │ Mailbox+AgentTool             │ CC P2  │ W6~7     │ ★★★  │
│13  │ 비디오팀 Phase 3              │ 기존   │ W7~8     │ ★★★  │
│14  │ 에이전트 오피스 CC메트릭       │ CC P2  │ W8       │ ★★☆  │
│15  │ KAIROS+프롬프트오케스트레이션  │ CC P3  │ M3+      │ ★★★  │
│16  │ Build to Delete               │ CC P3  │ M3+      │ ★★☆  │
│17  │ Chronos L2~3 + 환류사이클     │ 기존   │ M3+      │ ★★★  │
└────┴──────────────────────────────┴────────┴──────────┴────────┘

출처 구분:
  CC = 클로드 코드 유출 분석에서 도출
  기존 = 이전 세션에서 계획된 PENDING
  CC+기존 = 합체 가능 → 함께 진행!
```

---

## 4. 미해결 인프라 (별도 트랙)

```
📋 n8n 자격증명 에러 미해결
📋 CalDigit TS4 이더넷 미인식 → WiFi 사용 중 (A/S 필요)
📋 FLUX steps 28→10 미반영 원인 추적
📋 소스코드 딥 분석 미완료: orchestrator/router.js, worker/lib, ska/src Python
```

---

## 5. 코덱스 프롬프트 정리 (48건 → 활성/완료/폐기 분류 필요)

```
현재 docs/codex/ 에 48개 프롬프트 존재
  → 다음 세션에서 분류:
     ACTIVE (아직 실행 안 됨) / DONE (완료됨) / OBSOLETE (폐기)
  → 정리 후 깃에서 완료분 아카이브
```
