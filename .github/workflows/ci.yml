name: CI — Lint & Syntax Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  lint-and-check:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Check for secrets in code
        run: ./node_modules/.bin/tsx bots/hub/scripts/secret-leak-smoke.ts

      - name: TypeScript check
        run: npm run typecheck

      - name: Migration grant gate
        env:
          MIGRATION_GRANT_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: npm run -s migration:grant-gate

      - name: TypeScript Phase 1 build
        run: npm run build:ts

      - name: Syntax check (changed JS files)
        run: |
          echo "=== JS Syntax Check ==="
          ERRORS=0
          find bots packages -name "*.js" -not -path "*/node_modules/*" | while read f; do
            node --check "$f" 2>/dev/null || { echo "❌ $f"; ERRORS=$((ERRORS+1)); }
          done
          if [ "$ERRORS" -gt 0 ]; then
            echo "❌ $ERRORS files failed syntax check"
            exit 1
          fi
          echo "✅ All files passed"

      - name: Investment fill resolver smoke
        run: npm --prefix bots/investment run smoke:fill-resolver-dca

      # strict는 보안/필수 게이트 뒤 + 경고(비차단): strict debt를 가시화하되 CI 차단은 하지 않는다.
      - name: TypeScript strict check (warning, non-blocking)
        run: npm run typecheck:strict
        continue-on-error: true

  deploy:
    name: 운영 배포 (맥 스튜디오)
    needs: lint-and-check
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: self-hosted
    environment: production
    env:
      PROJECT_ROOT: /Users/alexlee/projects/ai-agent-system

    steps:
      - name: 배포 스크립트 실행
        run: |
          chmod +x scripts/deploy.sh
          PROJECT_ROOT="$PROJECT_ROOT" ./scripts/deploy.sh
